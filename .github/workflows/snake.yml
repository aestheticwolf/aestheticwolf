name: Generate Contribution Snake

on:
  schedule:
    - cron: "0 7 * * *"

  workflow_dispatch:

  push:
    branches:
      - main

jobs:
  generate:
    permissions:
      contents: write

    runs-on: ubuntu-latest

    steps:
      # ==========================================
      # Generate contribution snake
      # ==========================================

      - name: Generate Snake Animation
        uses: Platane/snk/svg-only@v3

        with:
          github_user_name: ${{ github.repository_owner }}

          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#22D3EE&color_dots=#0B1220,#164E63,#0891B2,#06B6D4,#67E8F9
            dist/github-contribution-grid-snake-dark.svg?color_snake=#F472B6&color_dots=#0D1117,#312E81,#4C1D95,#7C3AED,#C084FC

        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ==========================================
      # Push generated SVG files to output branch
      # ==========================================

      - name: Deploy Snake Animation
        uses: crazy-max/ghaction-github-pages@v4

        with:
          target_branch: output
          build_dir: dist

        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}